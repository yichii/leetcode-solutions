import java.util.ArrayList;
import java.util.Arrays;

class LongestConsecutive {
    public int longestConsecutive(int[] nums) {
        int n = 0;
        ArrayList<> numbers = new ArrayList<>(Arrays.asList(nums));
        for (int num : nums) {
            min = Arrays.stream(nums).min().getAsInt();
        }
        return n;
    }
}